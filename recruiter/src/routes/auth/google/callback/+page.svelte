<!--
	Never actually rendered — the server load always redirects. It exists
	because SvelteKit requires a page component alongside +page.server.ts, and
	it is what a user would see if the redirect were ever slow.
-->
<div class="min-h-screen flex items-center justify-center px-4">
	<p class="text-sm text-muted">Signing you in…</p>
</div>
